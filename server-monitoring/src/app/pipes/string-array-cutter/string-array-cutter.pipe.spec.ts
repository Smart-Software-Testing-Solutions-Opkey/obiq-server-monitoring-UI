import { StringArrayCutterPipe } from './string-array-cutter.pipe';

describe('StringArrayCutterPipe', () => {
  it('create an instance', () => {
    const pipe = new StringArrayCutterPipe();
    expect(pipe).toBeTruthy();
  });
});
