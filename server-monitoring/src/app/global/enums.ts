export enum NotificationType {
    none = "none",
    success = "success",
    info = "info",
    error = "error",
    warning = "warning",
  }
  
  export enum MsgBoxType {
    none = "none",
    success = "success",
    info = "info",
    error = "error",
    warning = "warning",
    confirm = "confirm",
    delete = "delete",
  }